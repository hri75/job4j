package ru.job4j.pseudo;

/**
 * Интерфейс, рисующий фигуру в псевдографике.
 */
public interface Shape {
    /**
     *  Метод должен возвращать фигуру в виде строки.
     * @return - строка с фигурой.
     */
    String draw();
}
